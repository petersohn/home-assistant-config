[project]
name = "test"
version = "0.1.0"
description = "Test runner for home-assistant-config"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pytest",
    "requests",
    "pyyaml",
    "python-dateutil",
    "psutil",
]