heating_pump:
  class: TemperatureController
  module: temperature_controller
  target:
    min_temperature: 0
    min_target: 60
    max_temperature: 10
    max_target: 50
  minimum: 30
  low_mode_start: 5
  low_mode_time: 2
  tolerance: 1
  sensor: sensor.furnace
  outside_sensor: sensor.outside
  availability: binary_sensor.available
  switch: input_boolean.temperature_controller_pump
