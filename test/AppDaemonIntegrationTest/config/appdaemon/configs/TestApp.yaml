global_modules:
    - mutex_graph
    - hass

locker:
    module: locker
    class: Locker
    global_dependencies:
      - mutex_graph
      - hass
    enable_logging: true

test:
    module: TestApp
    class: TestApp
    dependencies:
      - locker
    global_dependencies: mutex_graph
